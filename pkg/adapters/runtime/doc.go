// Package runtime is part of inferencecache-controller: the runtime adapter library
// that injects cache-backend config into engine pods (e.g. vLLM --kv-transfer-config,
// SGLang HiCache env) at reconcile time.
package runtime
