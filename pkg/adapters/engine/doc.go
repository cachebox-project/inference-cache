// Package engine is part of inferencecache-server: the engine KV-event hook that
// subscribes to vLLM/SGLang KV cache events and reports cache state to the server's
// index. Metadata only — never KV tensors or prompt text.
package engine
