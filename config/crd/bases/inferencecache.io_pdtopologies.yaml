---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: pdtopologies.inferencecache.io
spec:
  group: inferencecache.io
  names:
    kind: PDTopology
    listKind: PDTopologyList
    plural: pdtopologies
    shortNames:
    - pdt
    singular: pdtopology
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.prefillPools[*].name
      name: Prefill
      type: string
    - jsonPath: .spec.decodePools[*].name
      name: Decode
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: PDTopology is the Schema for prefill/decode topology declarations.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PDTopologySpec defines prefill/decode topology for phase-disaggregated
              serving.
            properties:
              acceleratorTypes:
                description: AcceleratorTypes declares the accelerator classes used
                  by the pools.
                items:
                  description: PDAcceleratorTypeSpec describes an accelerator class
                    used by a topology.
                  properties:
                    matchLabels:
                      additionalProperties:
                        type: string
                      description: MatchLabels identifies nodes or pods that expose
                        this accelerator type.
                      type: object
                    model:
                      description: Model is the accelerator model or SKU.
                      type: string
                    name:
                      description: Name is the accelerator type identifier.
                      type: string
                    vendor:
                      description: Vendor is the accelerator vendor or runtime family.
                      type: string
                  required:
                  - name
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              decodePools:
                description: DecodePools declares pools that can serve decode work.
                items:
                  description: PDPoolSpec declares a prefill or decode pool.
                  properties:
                    acceleratorType:
                      description: AcceleratorType references spec.acceleratorTypes
                        by name.
                      type: string
                    matchLabels:
                      additionalProperties:
                        type: string
                      description: MatchLabels selects pods or nodes belonging to
                        this pool.
                      type: object
                    name:
                      description: Name is the pool identifier.
                      type: string
                    replicas:
                      description: Replicas is the desired pool size.
                      format: int32
                      minimum: 0
                      type: integer
                  required:
                  - name
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              prefillPools:
                description: PrefillPools declares pools that can serve prefill work.
                items:
                  description: PDPoolSpec declares a prefill or decode pool.
                  properties:
                    acceleratorType:
                      description: AcceleratorType references spec.acceleratorTypes
                        by name.
                      type: string
                    matchLabels:
                      additionalProperties:
                        type: string
                      description: MatchLabels selects pods or nodes belonging to
                        this pool.
                      type: object
                    name:
                      description: Name is the pool identifier.
                      type: string
                    replicas:
                      description: Replicas is the desired pool size.
                      format: int32
                      minimum: 0
                      type: integer
                  required:
                  - name
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
            type: object
          status:
            description: PDTopologyStatus defines observed topology state.
            properties:
              conditions:
                description: Conditions describe the latest observations of the topology.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: ObservedGeneration is the latest generation observed
                  by the controller.
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
