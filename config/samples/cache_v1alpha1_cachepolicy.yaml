# Kubebuilder-generated minimum-viable sample: the smallest CachePolicy that
# applies cleanly against the current schema. Only wired fields are populated.
apiVersion: inferencecache.io/v1alpha1
kind: CachePolicy
metadata:
  labels:
    app.kubernetes.io/name: inference-cache
  name: cachepolicy-sample
spec:
  evictionTTL: 30m
  minimumPrefixTokens: 32
  lookupTimeoutMs: 20
