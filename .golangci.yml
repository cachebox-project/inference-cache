run:
  timeout: 5m
  tests: true

linters:
  enable:
    - errcheck
    - gofmt
    - govet
    - ineffassign
    - staticcheck
    - unused

issues:
  exclude-dirs:
    - bin
    - dist
    - .cache
